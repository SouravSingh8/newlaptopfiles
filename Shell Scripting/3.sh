# This script defines and sets two variables and then prints the value of the variables to the standard output

#!/bin/bash

#declaring and setting 2 variables
VARIABLE1=10
VARIABLE2="ten"

echo "$VARIABLE1"	#The echo command prints to the standard output. $ is used to refer to the value inside the variable
echo "$VARIABLE2"	
